// let soup= "he";

// let moup = soup? "yay ${soup}":"no";

let testScore = 79;
let myGrade = testScore > 89 ? " A"
:testScore > 79 ? "B"
:testScore > 69 ? "C"
:testScore > 59 ? "D"
:testScore > 49 ? "E"
:"F";

console.log(`my test grade is  ${testScore}`);